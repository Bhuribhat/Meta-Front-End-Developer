# word count

# lines of text, number of words, number of characters
wc sample.txt

# get line counts
wc -l sample.txt

# get word counts
wc -w sample.txt

# get character counts
wc -c sample.txt

# option only line and word
we -lw sample.txt

# input from keyboard (Ctrl + d to exit)
# then it will display the information
wc